# 🎨 Resumen Visual - Búsqueda Semántica con Supabase

## 🗂️ ¿Dónde se Guardan los Embeddings?

```
┌─────────────────────────────────────────────────────────────────┐
│                        SUPABASE DATABASE                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  📊 Tabla: busqueda_envioembedding                              │
│  ┌────────────────────────────────────────────────────────┐     │
│  │ id (PK)              │ 1                               │     │
│  │ envio_id (FK)        │ 123                             │     │
│  │ embedding_vector     │ [0.123, -0.456, 0.789, ...]    │ ⭐  │
│  │                      │ (1536 dimensiones)              │     │
│  │ texto_indexado       │ "HAWB: ABC123 | Comprador: ... │     │
│  │ fecha_generacion     │ 2025-11-21 10:30:00            │     │
│  │ modelo_usado         │ text-embedding-3-small         │     │
│  └────────────────────────────────────────────────────────┘     │
│                                                                  │
│  ✅ PERMANENTE: Se guarda cada vez que creas un envío          │
│  💰 COSTO: Una vez por envío (~$0.0002)                        │
│  📏 TAMAÑO: ~6 KB por embedding                                │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    MEMORIA RAM (Temporal)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  🔍 Embedding de Consulta                                       │
│  ┌────────────────────────────────────────────────────────┐     │
│  │ consulta_texto       │ "envíos pesados a Quito"       │     │
│  │ embedding_consulta   │ [0.321, -0.654, 0.987, ...]    │ 🔄  │
│  │                      │ (1536 dimensiones)              │     │
│  └────────────────────────────────────────────────────────┘     │
│                                                                  │
│  ❌ TEMPORAL: Solo existe durante la búsqueda                  │
│  💰 COSTO: Cada búsqueda (~$0.00002)                           │
│  ⏱️ DURACIÓN: ~500ms (luego se descarta)                       │
│                                                                  │
│  📝 Solo se guarda el HISTORIAL:                               │
│  ┌────────────────────────────────────────────────────────┐     │
│  │ consulta (texto)     │ ✅ Guardado                     │     │
│  │ fecha_busqueda       │ ✅ Guardado                     │     │
│  │ resultados_encontrados│ ✅ Guardado                    │     │
│  │ tiempo_respuesta     │ ✅ Guardado                     │     │
│  │ embedding_vector     │ ❌ NO se guarda                 │     │
│  └────────────────────────────────────────────────────────┘     │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flujo Completo de Datos

### 📤 CREAR ENVÍO (Embedding se guarda)

```
┌─────────────┐
│   Usuario   │
│  Crear Envío│
└──────┬──────┘
       │
       ▼
┌─────────────────────────────┐
│  Backend (Django)           │
│  generar_texto_envio()      │
│  "HAWB: ABC | Ciudad: ..."  │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  OpenAI API                 │
│  text-embedding-3-small     │
│  Genera vector [1536 dims]  │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  Supabase                   │
│  busqueda_envioembedding    │
│  ✅ GUARDADO PERMANENTE     │
└─────────────────────────────┘
```

### 🔍 BUSCAR ENVÍO (Embedding temporal)

```
┌─────────────┐
│   Usuario   │
│  "Búsqueda" │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────┐
│  Backend (Django)           │
│  consulta: "envíos pesados" │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  OpenAI API                 │
│  Genera embedding           │
│  [0.123, -0.456, ...]       │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  Memoria RAM                │
│  embedding_consulta (temp)  │ ⏱️ Existe solo aquí
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  Supabase                   │
│  SELECT embeddings          │
│  FROM busqueda_envioembedding│
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  Backend (NumPy)            │
│  calcular_similitudes()     │
│  - Cosine similarity        │
│  - Euclidean distance       │
│  - Dot product              │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  Resultados Ordenados       │
│  [envio1, envio2, ...]      │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────────┐
│  Frontend (Angular)         │
│  Muestra resultados         │
└─────────────────────────────┘
```

---

## 📊 Comparación Visual

```
┌─────────────────────────────────────────────────────────────────┐
│                    EMBEDDING DE ENVÍO                            │
├─────────────────────────────────────────────────────────────────┤
│  💾 GUARDADO: Sí, en Supabase                                   │
│  📍 UBICACIÓN: busqueda_envioembedding.embedding_vector         │
│  🔢 CANTIDAD: 1 por cada envío (reutilizable)                  │
│  💰 COSTO: $0.0002 por envío (una sola vez)                    │
│  ⏱️ GENERACIÓN: Al crear/importar envío                        │
│  🔄 REUTILIZACIÓN: Infinita (se usa en todas las búsquedas)    │
│  📏 TAMAÑO: 6 KB por embedding                                 │
│  🗑️ ELIMINACIÓN: Solo si eliminas el envío                     │
└─────────────────────────────────────────────────────────────────┘

                              VS

┌─────────────────────────────────────────────────────────────────┐
│                  EMBEDDING DE CONSULTA                           │
├─────────────────────────────────────────────────────────────────┤
│  💾 GUARDADO: No, solo en RAM                                   │
│  📍 UBICACIÓN: Variable temporal en memoria                     │
│  🔢 CANTIDAD: 1 por cada búsqueda (se descarta después)        │
│  💰 COSTO: $0.00002 por búsqueda (cada vez)                    │
│  ⏱️ GENERACIÓN: Cada vez que buscas                            │
│  🔄 REUTILIZACIÓN: 0 (se regenera siempre)                     │
│  📏 TAMAÑO: ~6 KB (pero solo existe ~500ms)                    │
│  🗑️ ELIMINACIÓN: Automática al terminar la búsqueda           │
└─────────────────────────────────────────────────────────────────┘
```

---

## 💰 Cálculo de Costos

```
ESCENARIO TÍPICO
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1️⃣ CARGA INICIAL (Una vez)
   └─ 1,000 envíos × $0.0002 = $0.20
   
2️⃣ USO MENSUAL
   └─ 10,000 búsquedas × $0.00002 = $0.20
   
3️⃣ NUEVOS ENVÍOS (Mensual)
   └─ 500 envíos nuevos × $0.0002 = $0.10

💵 TOTAL MENSUAL: ~$0.30/mes

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚡ OPTIMIZACIÓN: Los embeddings de envíos se generan
   UNA SOLA VEZ y se reutilizan infinitamente.
   
   Ejemplo: 1 envío = $0.0002
            Usado en 1000 búsquedas = $0.0002 (mismo costo!)
            
   VS
   
   Consultas: Cada búsqueda = $0.00002 (no se reutiliza)
```

---

## 🏗️ Arquitectura Simplificada

```
┌───────────────────────────────────────────────────────────┐
│                     ANGULAR FRONTEND                       │
│  [Input: "envíos pesados a Quito"] → [Resultados]        │
└─────────────────────┬─────────────────────────────────────┘
                      │ POST /api/busqueda/semantica/
                      ▼
┌───────────────────────────────────────────────────────────┐
│                    DJANGO BACKEND                          │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ views.py                                            │  │
│  │ - busqueda_semantica() ← Endpoint principal        │  │
│  │ - _generar_embedding() ← Crea embedding consulta   │  │
│  │ - _buscar_envios_similares() ← Busca matches       │  │
│  └─────────────────────────────────────────────────────┘  │
│  ┌─────────────────────────────────────────────────────┐  │
│  │ utils_embeddings.py                                 │  │
│  │ - generar_embedding_envio() ← Guarda en Supabase   │  │
│  │ - calcular_similitudes() ← Compara vectores        │  │
│  └─────────────────────────────────────────────────────┘  │
└─────┬────────────────────────┬────────────────────────────┘
      │                        │
      ▼                        ▼
┌─────────────┐     ┌─────────────────────────┐
│  OpenAI API │     │  SUPABASE               │
│             │     │  ┌───────────────────┐  │
│ Embeddings  │     │  │ envioembedding    │  │
│ API         │     │  │ ✅ embedding_vector│ │
│             │     │  └───────────────────┘  │
│ $0.02/1M    │     │  ┌───────────────────┐  │
│ tokens      │     │  │ busquedasemantica │  │
│             │     │  │ (historial)       │  │
└─────────────┘     │  └───────────────────┘  │
                    └─────────────────────────┘
```

---

## ✅ Cambios para Supabase

```diff
# backend/settings.py

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
-       'HOST': 'localhost',
+       'HOST': 'db.xxxxxxxxxxxxxx.supabase.co',
-       'PORT': '5432',
+       'PORT': '5432',
+       'OPTIONS': {
+           'sslmode': 'require',  # ⭐ AGREGAR ESTO
+       }
    }
}
```

```diff
# backend/.env

- DB_HOST=localhost
+ DB_HOST=db.xxxxxxxxxxxxxx.supabase.co
  
- DB_PASSWORD=local_password
+ DB_PASSWORD=tu_supabase_password
```

---

## 🎯 Resumen de 3 Puntos

1. **Embeddings de Envíos** → ✅ Se guardan en Supabase (`busqueda_envioembedding`)
   - Permanentes, reutilizables, generados una vez

2. **Embeddings de Consultas** → ❌ NO se guardan
   - Temporales, generados cada búsqueda, se descartan

3. **Para migrar a Supabase** → ⚠️ Solo agregar SSL
   - `OPTIONS = {'sslmode': 'require'}` en settings.py
   - Todo lo demás funciona igual

---

## 🚀 Próximos Pasos

```bash
# 1. Verificar configuración
python backend/verificar_supabase.py

# 2. Generar embeddings
python manage.py generar_embeddings_masivo --limite 10

# 3. Probar búsqueda
http://localhost:4200/busqueda-unificada
```

---

**📚 Más información:**
- Quick Start → `RESPUESTA_RAPIDA_SUPABASE.md`
- Migración → `MIGRACION_SUPABASE.md`
- Documentación completa → `GUIA_BUSQUEDA_SEMANTICA_COMPLETA.md`

